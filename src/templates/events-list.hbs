<h2>## {{day}}</h2>
{{#each events}}
<p class="
{{#if highlight}}highlight {{/if}}
{{#if done}}done{{/if}}
">
  - [{{#if done}}x{{else}} {{/if}}]
  {{beginTime}} - {{endTime}} {{{title}}}
  {{#if place}}(<span class="location-emoji">📍</span>{{place}}){{/if}}
</p>
{{/each}}
<br>
